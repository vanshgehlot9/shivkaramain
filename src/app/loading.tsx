import Preloader from "@/components/Preloader";

export default function Loading() {
    // We can reuse the Preloader component or create a simpler skeleton
    // Since Preloader is a full-screen loader, it works well here.
    return <Preloader />;
}
